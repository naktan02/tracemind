# Shared

로컬 agent와 중앙 서버가 함께 사용하는
공용 contract, domain entity, canonical 계산 규칙의 source of truth다.

처음 읽을 때는 아래 순서를 권장한다.

1. `shared/src/contracts/README.md`
2. `shared/src/contracts/*.py`
3. `shared/src/domain/entities/*`
