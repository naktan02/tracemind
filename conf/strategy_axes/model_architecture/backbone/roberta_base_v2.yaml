# @package paper_backbone

name: roberta_base_v2
model_id: FacebookAI/roberta-base
revision: main
tokenizer_model_id: ${paper_backbone.model_id}
tokenizer_revision: ${paper_backbone.revision}
cache_dir: hf_cache
trust_remote_code: false
pooling: mean
max_length: 256
classifier_dropout: 0.1
task_prefix: ""
