# @package local_update_profile

# PEFT text encoder update family 위에서 classifier head update를 만든다.
# FixMatch/FlexMatch/FedMatch 같은 SSL/FSSL method 세부값은 이 profile이 아니라
# method descriptor 또는 consistency_method leaf가 소유한다.
algorithm_profile_name: peft_classifier_update_v1
training_scope: adapter_only
training_backend_name: peft_classifier_trainer
validation_scorer_backend_name: peft_classifier_eval
validation_score_policy_name: null
validation_score_top_k: null
privacy_guard_name: noop
