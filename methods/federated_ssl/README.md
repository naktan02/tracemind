# Federated SSL Methods

`methods/federated_ssl/`는 SSL local update와 federated aggregation을 조합하는
composition layer다.

실험 entrypoint, Hydra config loading, report/artifact 저장은 `scripts/`에 남긴다.
