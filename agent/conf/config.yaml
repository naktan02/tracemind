defaults:
  - pipeline: default
  - embedding: mxbai_large
  - translation: nllb_200_distilled_600m
  - override /hydra/job_logging: disabled
  - override /hydra/hydra_logging: disabled
  - _self_

app:
  name: tracemind-agent
  stage: bootstrap

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
