backend: hash_debug
model_id: hash_debug
revision: debug
batch_size: 16
cache_dir: hf_cache
task_prefix: ""
hash_dim: 256
spec:
  _target_: shared.src.domain.value_objects.EmbeddingAdapterSpec
  backend: ${embedding.backend}
  model_id: ${embedding.model_id}
  revision: ${embedding.revision}
  device: ${runtime.device}
  batch_size: ${embedding.batch_size}
  cache_dir: ${embedding.cache_dir}
  task_prefix: ${embedding.task_prefix}
  hash_dim: ${embedding.hash_dim}
  local_files_only: ${runtime.local_files_only}
